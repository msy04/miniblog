<div class="contents row">
  <%= form_with(url: "/blogs/#{@blog.id}", method: :patch, local: true) do |form| %>
  <h3>
    編集する
  </h3>
  <%= form.text_area :text %>
  <%= form.submit "SENT" %>
  <% end %>
</div>
